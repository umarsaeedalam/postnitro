import type { Metadata } from "next";
// import NavMenu from "@/components/pieces/nav-menu";

export const metadata: Metadata = {
    title: 'Free AI Carousel Generator'
};

function Home() {
    return (
        <div className="w-full h-full flex items-center justify-center">
            XYZ
        </div>
    );
}

export default Home;
